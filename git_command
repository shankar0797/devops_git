shivashankar071997@gmail.com
Shiva@0797

#check existing branch:
 $ git branch

#change the branch
 $ git checkout <branch name>

#git clone (download the repo from github into local)
 $ git clone <git https code link>

# change push(upload)
 $ git add .
 $ git commit -m "user comment"
 $ git push

# update the repo file
 $ git pull origin <branch name>

# got back to previous pull(roll back)
 $ git stash

# create branch
 create from UI
 $ git branch <new_branch name>
 $ git checkout -b <new_branch name>
 

# branch merging
# version roll back
# pull request
# branch merge conflict



git status changes in git lab 
git add 
git confing -- list
git confog -- global username""
git commit -m menu"
git push origin bn
git init
git remote -v
git remote origin add 




---------------------------------------------------------------------------
git touch 1 2 3 to create file 
when the staus will be modified 
git add will add all the untracked file into the git

ex- git add. will add all untracked file 
git add 1 individual


git commit -m new "new files "
git rm filename is going to remove the file name
git mv 3 33 ur renaming a file from 3 to 33 
-----------to commit the changes -----
git add .
git commit -m "new file"
git push origin "filename"


---------------------------------------------------
git log -what all the changes we have done in our history here we will have unique commit id 
here contain- git id ,git message ,git time

git show commitid  

------to take changes from the gitlab
git pull origin main


-----vi l---to add content to the file 
 
git reset --hard all the changes that are modified are reverted
it will go to last pull 


----------------------
git checkout filename is to revert back the changes to the specific file 
------------------


git diff
---------------------------
action items 
aws account -ec2 instance - 
create sh (ssh -keygen) -> 5 
copy  /home/ubuntu /.ssh /id_rsa.pub

goto github ->

